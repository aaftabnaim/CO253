#include <stdio.h>              //include information about the standard library

int main(){                         //define a function called main that takes no argument values
    printf("hello World\n");    //main calls the library function printf to print the sequence of characters given
                                // \n represents the newline character
}

